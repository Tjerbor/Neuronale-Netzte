package utils;

/**
 * is not needed was intended to change values for
 * java so values are easier to compute and are the right values.
 */
public class global_variables {

    public static int decimal_precision = 16;
}
